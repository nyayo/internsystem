import React from 'react';
import { studentNavLinks } from '../../data/studentDashboardData';
import './StudentSideBar.css';

export default function StudentSideBar({ activeLink, onNavClick, isOpen, onToggle, pendingCount }) {
  return (
    <aside>
        
    </aside>
  );
}